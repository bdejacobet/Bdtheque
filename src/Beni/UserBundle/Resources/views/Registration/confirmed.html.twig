{% extends "BeniBdthequeBundle::Default/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
    {% block fos_user_content %}
        <div class="well">Félicitation {{ user.username }}, votre compte est maintenant actif</div>
        {% if app.session is not empty %}
            {% set targetUrl = app.session.get('_security.' ~ app.security.token.providerKey ~ '.target_path') %}
            {% if targetUrl is not empty %}
                <p><a href="{{ targetUrl }}" class="btn btn-primary">Retour sur la page d'origne</a></p>
            {% endif %}
        {% endif %}
    {% endblock fos_user_content %}
{% endblock %}
