{% extends "BeniBdthequeBundle::Default/layout.html.twig" %}

{% block title %}{{ parent() }} - liste des bd{% endblock %}

{% block body %}
    <h2 class="txt-white">Ma liste de BDs</h2>
    {% for oComicStrip in aComicStrip %}
        <article class="well">
            <h3 class="txt-black">{{ oComicStrip.title }}</h3>

            <p>({{ oComicStrip.author }})</p>
            <a href="{{ path('beni_bdtheque_comicstrip_details',{'idComicStrip':oComicStrip.id}) }}"
               class="btn btn-primary">Détails</a>
            <a href="{{ path('beni_bdtheque_comicstrip_edit', {'idComicStrip':oComicStrip.id}) }}"
               class="btn btn-primary">Modifier</a>
        </article>
    {% else %}
        <p>Pas (encore !) de BD</p>
    {% endfor %}
    <a href="{{ path('beni_bdtheque_comicstrip_create') }}" class="btn btn-primary">Ajouter une BD</a>
{% endblock %}
